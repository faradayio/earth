* make sure no airports are missing
* convert former/current verify blocks into table_warnings warn {...} blocks
* fix residence fuel price locatable stuff
* move some stuff from Country into other resources
